# NASA POWER D-103 Pre-Registration Charter

Status: frozen before the recorded D-103 run.

Dataset source:

```text
NASA POWER Daily point API, renewable-energy parameters, public official source.
```

Points:

```text
POWER_A, POWER_B, POWER_C
```

Period:

```text
2015-01-01 to 2024-12-31
```

Candidate:

```text
nasa_power_solar_resource_shortfall_zscore
```

Target:

```text
future low solar-resource shortfall over a seven-day horizon.
```

Acceptance:

```text
source audit passed;
at least two eligible points;
at least eight eligible windows;
candidate passes the frozen baseline/control acceptance gates.
```

Forbidden after result:

```text
changing thresholds;
removing windows;
renaming a failed run as success;
claiming broad deployment readiness;
claiming universal forecasting performance.
```

